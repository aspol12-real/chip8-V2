#include <iostream>
#include <raylib.h>
#include "ext/raygui.h"

#include "cpu.hpp"
#include "graphics.hpp"
#include "sound.hpp"

cpu chip8;

int main(int argc, char* argv[]) {


    //TODO: init window, for now use commandline args to point to rom (as always)

    /*
    while window open:
        - handle inputs
        - decrement timers
        - execute cpu
        - render screen
    */
    
}